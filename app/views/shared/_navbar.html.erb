<!-- filepath: c:\Users\User\Desktop\Edembergue\biblioteca\app\views\Shared\_navbar.html.erb -->
<% unless controller_name == "sessions" && (action_name == "new" || action_name == "create") %>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow mb-4">
  <div class="container">
    <%= link_to "📚 BiblioDon", root_path, class: "navbar-brand fw-bold" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
      aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
      <!-- Menu à esquerda -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Livros", books_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Empréstimos", loans_path, class: "nav-link" %>
        </li>
      </ul>

      <!-- Menu à direita -->
      <ul class="navbar-nav ms-auto align-items-center">
        <% if user_signed_in? %>
          <li class="nav-item me-3">
            <span class="navbar-text text-light">Olá, <%= current_user.name %></span>
          </li>
          <li class="nav-item">
            <%= link_to "Sair", destroy_user_session_path,
                  class: "btn btn-outline-light btn-sm",
                  data: { turbo_method: :delete, turbo_confirm: "Tem certeza que deseja sair?" } %>
          </li>
        <% else %>
          <li class="nav-item me-2">
            <%= link_to "Entrar", new_user_session_path, class: "btn btn-outline-light btn-sm me-2" %>
          </li>
          <li class="nav-item">
            <%= link_to "Cadastrar", new_user_registration_path, class: "btn btn-light btn-sm" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
<% end %>