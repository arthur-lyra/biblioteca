<!-- filepath: c:\Users\User\Desktop\Edembergue\biblioteca\app\views\layouts\application.html.erb -->
<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Biblioteca" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="Biblioteca online para empréstimos de livros">
    <meta name="keywords" content="livros, biblioteca, empréstimos">
    <meta name="author" content="Seu Nome">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="stylesheet" href="<%= asset_path 'application.css' %>" data-turbo-track="reload">
    <!-- Bootstrap CDN para garantir visual bonito -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>
  <body class="bg-light">
    <% flash.each do |key, value| %>
      <div class="alert alert-<%= key == "notice" ? "success" : "danger" %> alert-dismissible fade show mt-2 mx-2" role="alert">
        <%= value %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% end %>

    <!-- Navbar -->
    <header>
      <%= render "shared/navbar" %>
    </header>

    <!-- Conteúdo principal -->
    <main class="container py-5">
      <%= yield %>
    </main>

    <%= yield :scripts %>
  </body>
</html>